import { alpha } from '@mui/material/styles';

const palette = {
  mode: 'dark',
  primary: {
    dark: "#005B41",
    main: "#008170",
    light: "#64CCC5"
  }
};

export default palette;
